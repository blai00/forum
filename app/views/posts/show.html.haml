%h1= @post.title

%p= @post.content

#comments
    %h2
        Comments
    %h5
        =@post.comments.count 
        -if @post.comments.count ==1
            Comment
        -else
            Comments
        
    %h2 
    = render @post.comments
    
    %h3 Reply to Thread
    = render "comments/form"